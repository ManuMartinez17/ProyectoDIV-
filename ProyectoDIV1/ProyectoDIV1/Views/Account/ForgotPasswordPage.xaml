<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoDIV1.Views.Account.ForgotPasswordPage"
             Shell.FlyoutBehavior="Disabled" 
             Shell.BackgroundColor="{StaticResource Primary}">
    <Shell.TitleView>
        <StackLayout Orientation="Horizontal" Padding="10" VerticalOptions="Center">
            <Label Text="Recuperar Contraseña" TextColor="White" FontSize="Medium" FontAttributes="Bold" VerticalOptions="Center"  HorizontalTextAlignment="Center"></Label>
        </StackLayout>
    </Shell.TitleView>
    <ContentPage.Content>
        <StackLayout Padding="20"
                     VerticalOptions="Center">

            <Label Text="Olvidaste tu contraseña"
                     Style="{StaticResource HeaderLabel}"/>

            <Entry Placeholder="Email"
                   Text="{Binding Email}" />

            <Button Text="Enviar"
                    Command="{Binding ResetPasswordCommand}"
                    Style="{StaticResource PrimaryButton}"/>

            <Button Text="¿No tienes una cuenta? Registrarse"
                    Command="{Binding SignUpCommand}"
                    Style="{StaticResource SecondaryButton}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>